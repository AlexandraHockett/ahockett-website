module.exports = {
  content: [
    // Your content paths
  ],
  theme: {
    extend: {
      // Ensure gradients are enabled
      backgroundImage: {
        "gradient-to-t": "linear-gradient(to top, var(--tw-gradient-stops))",
      },
    },
  },
  plugins: [],
};
